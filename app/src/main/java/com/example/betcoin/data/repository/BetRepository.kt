package com.example.betcoin.data.repository

/**
 * Contract for bet lifecycle operations.
 *
 * Implementations handle bet creation, locking, resolution, cancellation,
 * admin operations, and history queries. Methods are defined in later tickets.
 */
interface BetRepository
